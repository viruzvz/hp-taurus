import './styles/main.scss'
import './styles/main.less'

import $ from 'jquery'
import 'popper.js'
import 'bootstrap'

window.$ = window.jQuery = $

const onDOMContentLoaded = function () {
  const $window = $(window)
  const $doc = $(document)
}

$(onDOMContentLoaded)
